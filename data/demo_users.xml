<odoo>
  <data noupdate="0">

    <!-- 👑 Admin User -->
    <record id="store_admin_user" model="res.users">
      <field name="name">Store Admin</field>
      <field name="login">storeadmin</field>
      <field name="password">admin123</field>
      <field name="groups_id" eval="[(4, ref('store_product_batch.group_store_batch_admin'))]"/>
    </record>

    <!-- 👤 Limited User -->
    <record id="store_limited_user" model="res.users">
      <field name="name">Store User</field>
      <field name="login">storeuser</field>
      <field name="password">user123</field>
      <field name="groups_id" eval="[(4, ref('store_product_batch.group_store_batch_user'))]"/>
    </record>

  </data>
</odoo>
